# Challenge: PostgreSQL + ingestion script
# Single command: docker-compose up

services:
  postgres:
    image: postgres:15-alpine
    container_name: sensor-data-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sensor_data
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sensor-network

  pipeline:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sensor-data-pipeline
    tty: true
    stdin_open: true
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      DB_NAME: sensor_data
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - sensor-network
    command: ["sh", "/app/scripts/run-pipeline.sh"]

volumes:
  postgres_data:

networks:
  sensor-network:
    driver: bridge
